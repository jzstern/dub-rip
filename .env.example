# =============================================================================
# Cobalt Configuration (Required for YouTube downloads)
# =============================================================================

# URL of your Cobalt instance
# Local development: Run Cobalt locally with Docker
COBALT_API_URL=http://localhost:9000

# Railway production: Use internal service-to-service communication
# COBALT_API_URL=http://cobalt.railway.internal:9000

# API key for authenticated Cobalt requests (generate with: uuidgen)
COBALT_API_KEY=your-api-key-uuid

# Public hostname for Cobalt tunnel URLs (if Cobalt returns public URLs)
# Only needed if your Cobalt instance is publicly accessible
# COBALT_TUNNEL_HOST=your-cobalt-hostname.up.railway.app

# =============================================================================
# Railway Runtime Configuration
# =============================================================================

# Required for yt-dlp fallback - installs Python in the runtime container
# Only needed when deploying to Railway
# RAILPACK_DEPLOY_APT_PACKAGES=python3

# =============================================================================
# Sentry Error Monitoring (Optional)
# =============================================================================

# Get your DSN from https://sentry.io/settings/projects/YOUR_PROJECT/keys/
# PUBLIC_SENTRY_DSN=https://your-key@your-org.ingest.sentry.io/your-project-id
# SENTRY_DSN=https://your-key@your-org.ingest.sentry.io/your-project-id
